(ns httpfoo.core-test
  (:use clojure.test
        httpfoo.core))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
(deftest print-test
  (testing "FIXME, I fail."
    (is (httpfoo.core/foo) )))
